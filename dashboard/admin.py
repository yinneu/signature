from django.contrib import admin
# from .models import time_use, signature, length_frequency, tel_network, flag_count
# Register your models here.

# admin.site.register(time_use)
# admin.site.register(signature)
# admin.site.register(length_frequency)
# admin.site.register(tel_network)
# admin.site.register(flag_count)
